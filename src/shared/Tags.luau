--!strict
-- ReplicatedStorage.Shared.Tags
-- CollectionService tag names as constants, so a typo becomes a require error instead of a
-- silently-broken tag lookup.
local Tags = {
	Mycomorph = "Mycomorph",
	Structure = "Structure",
	ElectricFence = "ElectricFence",
}

return Tags
